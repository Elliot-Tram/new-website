---
import { SITE_TITLE } from '../consts';
const today = new Date();
---

<footer>
	<div class="footer-inner">
		<div class="footer-brand">
			<div class="brand-row">
				<svg width="24" height="24" viewBox="0 0 28 28" fill="none" aria-hidden="true">
					<rect width="28" height="28" rx="8" fill="var(--accent)"/>
					<path d="M8 14L12 10L16 14L20 10" stroke="white" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"/>
					<path d="M8 18L12 14L16 18L20 14" stroke="white" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round" opacity="0.6"/>
				</svg>
				<span class="brand-name">{SITE_TITLE}</span>
			</div>
			<p class="brand-desc">
				La référence française des outils SaaS pour le marketing, la croissance et les ventes.
			</p>
		</div>

		<div class="footer-links">
			<div class="links-group">
				<h4 class="links-title">Outils</h4>
				<a href="/outils">Tous les outils</a>
				<a href="/outils?category=CRM">CRM</a>
				<a href="/outils?category=Email Marketing">Email Marketing</a>
				<a href="/outils?category=Analytics">Analytics</a>
			</div>
			<div class="links-group">
				<h4 class="links-title">Contenu</h4>
				<a href="/blog">Blog</a>
				<a href="/about">À propos</a>
			</div>
		</div>
	</div>

	<div class="footer-bottom">
		<p class="footer-copy">© {today.getFullYear()} {SITE_TITLE} — Tous droits réservés.</p>
		<p class="footer-made">Fait avec ☕ en France</p>
	</div>
</footer>

<style>
	footer {
		margin-top: 5rem;
		border-top: 1px solid var(--border-color);
		background: var(--bg-secondary);
	}

	.footer-inner {
		max-width: 1200px;
		margin: 0 auto;
		padding: 3.5rem 1.5rem 2.5rem;
		display: grid;
		grid-template-columns: 1fr auto;
		gap: 3rem;
	}

	/* ─── Brand column ─── */
	.footer-brand {
		max-width: 320px;
	}

	.brand-row {
		display: flex;
		align-items: center;
		gap: 0.625rem;
		margin-bottom: 0.875rem;
	}

	.brand-name {
		font-family: var(--font-display, "Bricolage Grotesque", system-ui);
		font-weight: 700;
		font-size: 1.0625rem;
		letter-spacing: -0.025em;
		color: var(--text-primary);
	}

	.brand-desc {
		font-size: 0.875rem;
		color: var(--text-muted);
		line-height: 1.6;
		margin: 0;
	}

	/* ─── Links columns ─── */
	.footer-links {
		display: flex;
		gap: 3rem;
	}

	.links-group {
		display: flex;
		flex-direction: column;
		gap: 0.5rem;
	}

	.links-title {
		font-family: var(--font-display, "Bricolage Grotesque", system-ui);
		font-size: 0.75rem;
		font-weight: 600;
		text-transform: uppercase;
		letter-spacing: 0.07em;
		color: var(--text-muted);
		margin-bottom: 0.375rem;
	}

	.links-group a {
		font-size: 0.875rem;
		color: var(--text-secondary);
		text-decoration: none;
		transition: color 0.15s ease;
		line-height: 1.8;
	}

	.links-group a:hover {
		color: var(--accent);
	}

	/* ─── Bottom bar ─── */
	.footer-bottom {
		max-width: 1200px;
		margin: 0 auto;
		padding: 1.25rem 1.5rem;
		border-top: 1px solid var(--border-light);
		display: flex;
		justify-content: space-between;
		align-items: center;
		gap: 1rem;
	}

	.footer-copy,
	.footer-made {
		font-size: 0.8125rem;
		color: var(--text-muted);
		margin: 0;
	}

	@media (max-width: 768px) {
		.footer-inner {
			grid-template-columns: 1fr;
			padding: 2.5rem 1rem 2rem;
			gap: 2rem;
		}

		.footer-brand {
			max-width: 100%;
		}

		.footer-links {
			gap: 2rem;
		}

		.footer-bottom {
			flex-direction: column;
			text-align: center;
			padding: 1rem;
		}
	}
</style>
